/* =============================================================================
   config.js
   ไฟล์นี้เก็บ "ค่าตั้งต้น" ของแอพทั้งหมด แก้ตรงนี้ที่เดียว ไม่ต้องไปหาในไฟล์อื่น
   ============================================================================= */

const CONFIG = {

  // 🔑 STEP A: ใส่ Google Maps API Key ของคุณตรงนี้
  //    วิธีขอฟรี: https://console.cloud.google.com/google/maps-apis
  //    -> สร้างโปรเจกต์ -> เปิดใช้งาน "Maps JavaScript API" และ "Directions API"
  //    -> ไปที่ Credentials -> Create Credentials -> API Key -> คัดลอกมาวางแทนบรรทัดล่าง
  GOOGLE_MAPS_API_KEY: "YOUR_API_KEY_HERE",

  // 📍 STEP B: จุดศูนย์กลางแผนที่ตอนเปิดแอพครั้งแรก (พิกัดกลางวิทยาลัย)
  //    วิธีหา: เปิด Google Maps จริง -> คลิกขวาตรงกลางวิทยาลัย -> คัดลอกพิกัดที่ขึ้นมา
  MAP_CENTER: { lat: 13.4150, lng: 99.9850 },

  // 🔍 STEP C: ระดับการซูมตอนเปิดแอพ (ยิ่งมาก ยิ่งซูมเข้า ปกติใช้ 17-19 สำหรับพื้นที่ขนาดวิทยาลัย)
  MAP_ZOOM: 18,

  // 🚶 โหมดการเดินทางตอนกดปุ่ม "นำทาง" — เลือกได้: WALKING (เดิน) / DRIVING (ขับรถ) / BICYCLING (จักรยาน)
  //    ภายในวิทยาลัยแนะนำ WALKING เพราะระยะทางสั้น
  TRAVEL_MODE: "WALKING",

};
